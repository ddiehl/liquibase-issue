# sample Project to show liquibase issue with CustomTaskChange
Thow project was created as a sample for a liquibase issue with CustomTaskChange.

to execute it run: 
```./gradlew clean build :migrations:build update```